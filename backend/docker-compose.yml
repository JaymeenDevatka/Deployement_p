version: "3.9"

services:
  backend:
    build: .
    container_name: notes_backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    command: gunicorn -w 2 -b 0.0.0.0:8000 app.main:create_app()
